> _Lane: 🛠 Pipeline._

# `transcripts/`

One `.md` per recording, `[hh:mm:ss]` timestamps preserved. **Immutable source of truth** — never edited after transcription. Drift mitigations rely on this: any analysis / summary cites a `[ts]` anchor pointing back here. Files are gitignored.
